import Storage from '@config/Storage';
import Theme from '@config/Theme';
import Url from '@config/Url';

export default {
  url: Url,
  theme: Theme,
  lang: 'id',
  storage: Storage,
  toast: {
    duration: 3000,
    type: 'LONG',
  },
};
